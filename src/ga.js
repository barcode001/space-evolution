

// (Optional to add custom events later)

// export const sendEvent = (action, params = {}) => {
//   if (typeof window.gtag === 'function') {
//     window.gtag("event", action, params);
//   }
// };